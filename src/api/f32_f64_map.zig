// Code generated by mapdb-codegen. DO NOT EDIT.

const std = @import("std");

/// Comptime interface for read-only maps from `f32` keys to `f64` values.
///
/// Equivalent to:
///   Go:   F32F64MapIterable interface
///   Rust: F32F64Map trait
///   Java: F32F64Map interface
pub fn assertF32F64Map(comptime T: type) void {
    comptime {
        if (!@hasDecl(T, "len")) @compileError(@typeName(T) ++ " missing len()");
        if (!@hasDecl(T, "isEmpty")) @compileError(@typeName(T) ++ " missing isEmpty()");
        if (!@hasDecl(T, "containsKey")) @compileError(@typeName(T) ++ " missing containsKey()");
        if (!@hasDecl(T, "get")) @compileError(@typeName(T) ++ " missing get()");
    }
}

/// Comptime interface for mutable maps from `f32` to `f64`.
pub fn assertF32F64MutableMap(comptime T: type) void {
    assertF32F64Map(T);
    comptime {
        if (!@hasDecl(T, "put")) @compileError(@typeName(T) ++ " missing put()");
        if (!@hasDecl(T, "remove")) @compileError(@typeName(T) ++ " missing remove()");
        if (!@hasDecl(T, "clear")) @compileError(@typeName(T) ++ " missing clear()");
    }
}

// Compile-time verification: every concrete `f32` → `f64` map type
// satisfies the matching read-only and (where applicable) mutable interface.
//
// Imports go through per-package index files — the index is the swap point
// for alternative implementations. See docs/zig/interface-redesign.md.
comptime {
    const hashmap = @import("../hashmap/hashmap.zig");
    const treemap = @import("../treemap/treemap.zig");
    const immutable = @import("../immutable/immutable.zig");

    const HashMap = hashmap.f32_f64_hash_map.F32F64HashMap;
    assertF32F64MutableMap(HashMap);

    const TreeMap = treemap.f32_f64_tree_map.F32F64TreeMap;
    assertF32F64MutableMap(TreeMap);

    const HashBiMap = hashmap.f32_f64_hash_bi_map.F32F64HashBiMap;
    assertF32F64MutableMap(HashBiMap);

    const ImmutableHashMap = immutable.immutable_f32_f64_hash_map.ImmutableF32F64HashMap;
    assertF32F64Map(ImmutableHashMap);
}
