// Code generated by mapdb-codegen. DO NOT EDIT.

const std = @import("std");

/// Comptime interface for read-only maps from `bool` keys to `i16` values.
///
/// Equivalent to:
///   Go:   BoolI16MapIterable interface
///   Rust: BoolI16Map trait
///   Java: BoolI16Map interface
pub fn assertBoolI16Map(comptime T: type) void {
    comptime {
        if (!@hasDecl(T, "len")) @compileError(@typeName(T) ++ " missing len()");
        if (!@hasDecl(T, "isEmpty")) @compileError(@typeName(T) ++ " missing isEmpty()");
        if (!@hasDecl(T, "containsKey")) @compileError(@typeName(T) ++ " missing containsKey()");
        if (!@hasDecl(T, "get")) @compileError(@typeName(T) ++ " missing get()");
    }
}

/// Comptime interface for mutable maps from `bool` to `i16`.
pub fn assertBoolI16MutableMap(comptime T: type) void {
    assertBoolI16Map(T);
    comptime {
        if (!@hasDecl(T, "put")) @compileError(@typeName(T) ++ " missing put()");
        if (!@hasDecl(T, "remove")) @compileError(@typeName(T) ++ " missing remove()");
        if (!@hasDecl(T, "clear")) @compileError(@typeName(T) ++ " missing clear()");
    }
}

// Compile-time verification: every concrete `bool` → `i16` map type
// satisfies the matching read-only and (where applicable) mutable interface.
//
// Imports go through per-package index files — the index is the swap point
// for alternative implementations. See docs/zig/interface-redesign.md.
comptime {
    const hashmap = @import("../hashmap/hashmap.zig");
    const treemap = @import("../treemap/treemap.zig");
    const immutable = @import("../immutable/immutable.zig");

    const HashMap = hashmap.bool_i16_hash_map.BoolI16HashMap;
    assertBoolI16MutableMap(HashMap);

    const TreeMap = treemap.bool_i16_tree_map.BoolI16TreeMap;
    assertBoolI16MutableMap(TreeMap);

    const HashBiMap = hashmap.bool_i16_hash_bi_map.BoolI16HashBiMap;
    assertBoolI16MutableMap(HashBiMap);

    const ImmutableHashMap = immutable.immutable_bool_i16_hash_map.ImmutableBoolI16HashMap;
    assertBoolI16Map(ImmutableHashMap);
}
