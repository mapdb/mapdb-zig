// Code generated by mapdb-codegen. DO NOT EDIT.

const std = @import("std");

/// Comptime interface for read-only maps from `bool` keys to `i8` values.
///
/// Equivalent to:
///   Go:   BoolI8MapIterable interface
///   Rust: BoolI8Map trait
///   Java: BoolI8Map interface
pub fn assertBoolI8Map(comptime T: type) void {
    comptime {
        if (!@hasDecl(T, "len")) @compileError(@typeName(T) ++ " missing len()");
        if (!@hasDecl(T, "isEmpty")) @compileError(@typeName(T) ++ " missing isEmpty()");
        if (!@hasDecl(T, "containsKey")) @compileError(@typeName(T) ++ " missing containsKey()");
        if (!@hasDecl(T, "get")) @compileError(@typeName(T) ++ " missing get()");
    }
}

/// Comptime interface for mutable maps from `bool` to `i8`.
pub fn assertBoolI8MutableMap(comptime T: type) void {
    assertBoolI8Map(T);
    comptime {
        if (!@hasDecl(T, "put")) @compileError(@typeName(T) ++ " missing put()");
        if (!@hasDecl(T, "remove")) @compileError(@typeName(T) ++ " missing remove()");
        if (!@hasDecl(T, "clear")) @compileError(@typeName(T) ++ " missing clear()");
    }
}

// Compile-time verification: every concrete `bool` → `i8` map type
// satisfies the matching read-only and (where applicable) mutable interface.
//
// Imports go through per-package index files — the index is the swap point
// for alternative implementations. See docs/zig/interface-redesign.md.
comptime {
    const hashmap = @import("../hashmap/hashmap.zig");
    const treemap = @import("../treemap/treemap.zig");
    const immutable = @import("../immutable/immutable.zig");

    const HashMap = hashmap.bool_i8_hash_map.BoolI8HashMap;
    assertBoolI8MutableMap(HashMap);

    const TreeMap = treemap.bool_i8_tree_map.BoolI8TreeMap;
    assertBoolI8MutableMap(TreeMap);

    const HashBiMap = hashmap.bool_i8_hash_bi_map.BoolI8HashBiMap;
    assertBoolI8MutableMap(HashBiMap);

    const ImmutableHashMap = immutable.immutable_bool_i8_hash_map.ImmutableBoolI8HashMap;
    assertBoolI8Map(ImmutableHashMap);
}
