// Code generated by mapdb-codegen. DO NOT EDIT.

const std = @import("std");

/// Comptime interface for read-only collections of `bool` values.
///
/// Any struct that has these methods can be used where an BoolCollection is expected.
/// Use `assertBoolCollection(T)` to verify at compile time.
///
/// Equivalent to:
///   Go:   BoolIterable interface
///   Rust: BoolCollection trait
///   Java: BoolIterable interface
pub fn assertBoolCollection(comptime T: type) void {
    // Required methods for read-only collection
    comptime {
        // fn len(*const T) usize
        if (!@hasDecl(T, "len")) @compileError(@typeName(T) ++ " missing len()");
        // fn isEmpty(*const T) bool
        if (!@hasDecl(T, "isEmpty")) @compileError(@typeName(T) ++ " missing isEmpty()");
        // fn contains(*const T, bool) bool
        if (!@hasDecl(T, "contains")) @compileError(@typeName(T) ++ " missing contains()");
    }
}

/// Comptime interface for mutable collections of `bool` values.
///
/// Extends BoolCollection with mutation operations.
///
/// Equivalent to:
///   Go:   BoolMutableCollection interface
///   Rust: BoolMutableCollection trait
///   Java: MutableBoolCollection interface
pub fn assertBoolMutableCollection(comptime T: type) void {
    // Must satisfy read-only interface first
    assertBoolCollection(T);

    comptime {
        // fn clear(*T) void
        if (!@hasDecl(T, "clear")) @compileError(@typeName(T) ++ " missing clear()");
        // fn deinit(*T) void
        if (!@hasDecl(T, "deinit")) @compileError(@typeName(T) ++ " missing deinit()");
    }
}

// ── Category interfaces — mirror Java's IntList/IntSet/IntBag/IntStack ──

/// Read-only ordered list with positional access. `get(index)` returns ?bool.
pub fn assertBoolList(comptime T: type) void {
    assertBoolCollection(T);
    comptime {
        if (!@hasDecl(T, "get")) @compileError(@typeName(T) ++ " missing get()");
    }
}

/// Mutable ordered list. Adds `add` (append) and `set` (positional replace).
pub fn assertBoolMutableList(comptime T: type) void {
    assertBoolList(T);
    assertBoolMutableCollection(T);
    comptime {
        if (!@hasDecl(T, "add")) @compileError(@typeName(T) ++ " missing add()");
        if (!@hasDecl(T, "set")) @compileError(@typeName(T) ++ " missing set()");
    }
}

/// Read-only set marker — uniqueness implied; no extra methods of its own.
pub fn assertBoolSet(comptime T: type) void {
    assertBoolCollection(T);
}

/// Mutable set. `add(value) bool` returns true if the value was newly inserted.
pub fn assertBoolMutableSet(comptime T: type) void {
    assertBoolSet(T);
    assertBoolMutableCollection(T);
    comptime {
        if (!@hasDecl(T, "add")) @compileError(@typeName(T) ++ " missing add()");
    }
}

/// Read-only multiset (bag) with occurrence counts.
pub fn assertBoolBag(comptime T: type) void {
    assertBoolCollection(T);
    comptime {
        if (!@hasDecl(T, "occurrencesOf")) @compileError(@typeName(T) ++ " missing occurrencesOf()");
        if (!@hasDecl(T, "sizeDistinct")) @compileError(@typeName(T) ++ " missing sizeDistinct()");
    }
}

/// Mutable bag. `add(value)` adds one occurrence.
pub fn assertBoolMutableBag(comptime T: type) void {
    assertBoolBag(T);
    assertBoolMutableCollection(T);
    comptime {
        if (!@hasDecl(T, "add")) @compileError(@typeName(T) ++ " missing add()");
    }
}

/// Read-only LIFO stack. `peek()` returns the top element.
pub fn assertBoolStack(comptime T: type) void {
    assertBoolCollection(T);
    comptime {
        if (!@hasDecl(T, "peek")) @compileError(@typeName(T) ++ " missing peek()");
    }
}

/// Mutable LIFO stack. Adds `push` and `pop`.
pub fn assertBoolMutableStack(comptime T: type) void {
    assertBoolStack(T);
    assertBoolMutableCollection(T);
    comptime {
        if (!@hasDecl(T, "push")) @compileError(@typeName(T) ++ " missing push()");
        if (!@hasDecl(T, "pop")) @compileError(@typeName(T) ++ " missing pop()");
    }
}

// Compile-time verification that every concrete collection type for `bool`
// satisfies the matching read-only and (where applicable) mutable interface.
// If any implementation drifts this block fails to compile.
//
// Imports go through the per-package index files (arraylist.zig, hashset.zig,
// etc.) rather than directly naming implementation files. This means the
// index file is the swap point — to use a different backing implementation,
// change which file the index re-exports. Both user code and this drift
// guard pick up the change automatically.
comptime {
    const arraylist = @import("../arraylist/arraylist.zig");
    const hashset = @import("../hashset/hashset.zig");
    const treeset = @import("../treeset/treeset.zig");
    const bag = @import("../bag/bag.zig");
    const stack = @import("../stack/stack.zig");
    const immutable = @import("../immutable/immutable.zig");

    // ArrayList — List + MutableList
    const ArrayList = arraylist.bool_array_list.BoolArrayList;
    assertBoolMutableCollection(ArrayList);
    assertBoolMutableList(ArrayList);

    // HashSet / TreeSet — Set + MutableSet
    const HashSet = hashset.bool_hash_set.BoolHashSet;
    assertBoolMutableCollection(HashSet);
    assertBoolMutableSet(HashSet);
    const TreeSet = treeset.bool_tree_set.BoolTreeSet;
    assertBoolMutableCollection(TreeSet);
    assertBoolMutableSet(TreeSet);

    // HashBag / TreeBag — Bag + MutableBag
    const HashBag = bag.bool_hash_bag.BoolHashBag;
    assertBoolMutableCollection(HashBag);
    assertBoolMutableBag(HashBag);
    const TreeBag = bag.bool_tree_bag.BoolTreeBag;
    assertBoolMutableCollection(TreeBag);
    assertBoolMutableBag(TreeBag);

    // ArrayStack — Stack + MutableStack
    const ArrayStack = stack.bool_array_stack.BoolArrayStack;
    assertBoolMutableCollection(ArrayStack);
    assertBoolMutableStack(ArrayStack);

    // Immutable types — read-only category trait only
    const ImmutableArrayList = immutable.immutable_bool_array_list.ImmutableBoolArrayList;
    assertBoolCollection(ImmutableArrayList);
    assertBoolList(ImmutableArrayList);

    const ImmutableHashSet = immutable.immutable_bool_hash_set.ImmutableBoolHashSet;
    assertBoolCollection(ImmutableHashSet);
    assertBoolSet(ImmutableHashSet);

    const ImmutableHashBag = immutable.immutable_bool_hash_bag.ImmutableBoolHashBag;
    assertBoolCollection(ImmutableHashBag);
    assertBoolBag(ImmutableHashBag);

    const ImmutableArrayStack = immutable.immutable_bool_array_stack.ImmutableBoolArrayStack;
    assertBoolCollection(ImmutableArrayStack);
    assertBoolStack(ImmutableArrayStack);
}

test "assertBoolCollection comptime check" {
    // This test passes if it compiles — the comptime block above does the real checking.
    const arraylist = @import("../arraylist/arraylist.zig");
    assertBoolCollection(arraylist.bool_array_list.BoolArrayList);
}
