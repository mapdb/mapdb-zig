// Code generated by mapdb-codegen. DO NOT EDIT.

const std = @import("std");

/// Comptime interface for read-only maps from `f32` keys to `i64` values.
///
/// Equivalent to:
///   Go:   F32I64MapIterable interface
///   Rust: F32I64Map trait
///   Java: F32I64Map interface
pub fn assertF32I64Map(comptime T: type) void {
    comptime {
        if (!@hasDecl(T, "len")) @compileError(@typeName(T) ++ " missing len()");
        if (!@hasDecl(T, "isEmpty")) @compileError(@typeName(T) ++ " missing isEmpty()");
        if (!@hasDecl(T, "containsKey")) @compileError(@typeName(T) ++ " missing containsKey()");
        if (!@hasDecl(T, "get")) @compileError(@typeName(T) ++ " missing get()");
    }
}

/// Comptime interface for mutable maps from `f32` to `i64`.
pub fn assertF32I64MutableMap(comptime T: type) void {
    assertF32I64Map(T);
    comptime {
        if (!@hasDecl(T, "put")) @compileError(@typeName(T) ++ " missing put()");
        if (!@hasDecl(T, "remove")) @compileError(@typeName(T) ++ " missing remove()");
        if (!@hasDecl(T, "clear")) @compileError(@typeName(T) ++ " missing clear()");
    }
}

// Compile-time verification: every concrete `f32` → `i64` map type
// satisfies the matching read-only and (where applicable) mutable interface.
//
// Imports go through per-package index files — the index is the swap point
// for alternative implementations. See docs/zig/interface-redesign.md.
comptime {
    const hashmap = @import("../hashmap/hashmap.zig");
    const treemap = @import("../treemap/treemap.zig");
    const immutable = @import("../immutable/immutable.zig");

    const HashMap = hashmap.f32_i64_hash_map.F32I64HashMap;
    assertF32I64MutableMap(HashMap);

    const TreeMap = treemap.f32_i64_tree_map.F32I64TreeMap;
    assertF32I64MutableMap(TreeMap);

    const HashBiMap = hashmap.f32_i64_hash_bi_map.F32I64HashBiMap;
    assertF32I64MutableMap(HashBiMap);

    const ImmutableHashMap = immutable.immutable_f32_i64_hash_map.ImmutableF32I64HashMap;
    assertF32I64Map(ImmutableHashMap);
}
