// Code generated by mapdb-codegen. DO NOT EDIT.

const std = @import("std");

/// Comptime interface for read-only maps from `u21` keys to `i32` values.
///
/// Equivalent to:
///   Go:   CharI32MapIterable interface
///   Rust: CharI32Map trait
///   Java: CharI32Map interface
pub fn assertCharI32Map(comptime T: type) void {
    comptime {
        if (!@hasDecl(T, "len")) @compileError(@typeName(T) ++ " missing len()");
        if (!@hasDecl(T, "isEmpty")) @compileError(@typeName(T) ++ " missing isEmpty()");
        if (!@hasDecl(T, "containsKey")) @compileError(@typeName(T) ++ " missing containsKey()");
        if (!@hasDecl(T, "get")) @compileError(@typeName(T) ++ " missing get()");
    }
}

/// Comptime interface for mutable maps from `u21` to `i32`.
pub fn assertCharI32MutableMap(comptime T: type) void {
    assertCharI32Map(T);
    comptime {
        if (!@hasDecl(T, "put")) @compileError(@typeName(T) ++ " missing put()");
        if (!@hasDecl(T, "remove")) @compileError(@typeName(T) ++ " missing remove()");
        if (!@hasDecl(T, "clear")) @compileError(@typeName(T) ++ " missing clear()");
    }
}

// Compile-time verification: every concrete `u21` → `i32` map type
// satisfies the matching read-only and (where applicable) mutable interface.
//
// Imports go through per-package index files — the index is the swap point
// for alternative implementations. See docs/zig/interface-redesign.md.
comptime {
    const hashmap = @import("../hashmap/hashmap.zig");
    const treemap = @import("../treemap/treemap.zig");
    const immutable = @import("../immutable/immutable.zig");

    const HashMap = hashmap.char_i32_hash_map.CharI32HashMap;
    assertCharI32MutableMap(HashMap);

    const TreeMap = treemap.char_i32_tree_map.CharI32TreeMap;
    assertCharI32MutableMap(TreeMap);

    const HashBiMap = hashmap.char_i32_hash_bi_map.CharI32HashBiMap;
    assertCharI32MutableMap(HashBiMap);

    const ImmutableHashMap = immutable.immutable_char_i32_hash_map.ImmutableCharI32HashMap;
    assertCharI32Map(ImmutableHashMap);
}
