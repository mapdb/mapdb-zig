// Code generated by mapdb-codegen. DO NOT EDIT.

const std = @import("std");

/// Immutable pair of (`f32`, `bool`).
pub const F32BoolPair = struct {
    first: f32,
    second: bool,

    pub fn new(first: f32, second: bool) F32BoolPair {
        return .{ .first = first, .second = second };
    }

    pub fn one(self: F32BoolPair) f32 {
        return self.first;
    }

    pub fn two(self: F32BoolPair) bool {
        return self.second;
    }

    pub fn swap(self: F32BoolPair) BoolF32Pair {
        return .{ .first = self.second, .second = self.first };
    }

    pub fn eql(self: F32BoolPair, other: F32BoolPair) bool {
        return (@as(u32, @bitCast(self.first)) == @as(u32, @bitCast(other.first))) and (self.second == other.second);
    }

    pub fn format(self: F32BoolPair, comptime _: []const u8, _: std.fmt.FormatOptions, writer: anytype) !void {
        try std.fmt.format(writer, "({}, {})", .{ self.first, self.second });
    }
};

const BoolF32Pair = @import("bool_f32_pair.zig").BoolF32Pair;

test "F32BoolPair: one and two" {
    const p = F32BoolPair.new(1.0, false);
    try std.testing.expectEqual(1.0, p.one());
    try std.testing.expectEqual(false, p.two());
}

test "F32BoolPair: eql" {
    const p1 = F32BoolPair.new(1.0, false);
    const p2 = F32BoolPair.new(1.0, false);
    const p3 = F32BoolPair.new(2.0, true);
    try std.testing.expect(p1.eql(p2));
    try std.testing.expect(!p1.eql(p3));
}
