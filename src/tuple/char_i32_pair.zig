// Code generated by mapdb-codegen. DO NOT EDIT.

const std = @import("std");

/// Immutable pair of (`u21`, `i32`).
pub const CharI32Pair = struct {
    first: u21,
    second: i32,

    pub fn new(first: u21, second: i32) CharI32Pair {
        return .{ .first = first, .second = second };
    }

    pub fn one(self: CharI32Pair) u21 {
        return self.first;
    }

    pub fn two(self: CharI32Pair) i32 {
        return self.second;
    }

    pub fn swap(self: CharI32Pair) I32CharPair {
        return .{ .first = self.second, .second = self.first };
    }

    pub fn eql(self: CharI32Pair, other: CharI32Pair) bool {
        return (self.first == other.first) and (self.second == other.second);
    }

    pub fn format(self: CharI32Pair, comptime _: []const u8, _: std.fmt.FormatOptions, writer: anytype) !void {
        try std.fmt.format(writer, "({}, {})", .{ self.first, self.second });
    }
};

const I32CharPair = @import("i32_char_pair.zig").I32CharPair;

test "CharI32Pair: one and two" {
    const p = CharI32Pair.new('a', 2);
    try std.testing.expectEqual('a', p.one());
    try std.testing.expectEqual(2, p.two());
}

test "CharI32Pair: eql" {
    const p1 = CharI32Pair.new('a', 2);
    const p2 = CharI32Pair.new('a', 2);
    const p3 = CharI32Pair.new('b', 1);
    try std.testing.expect(p1.eql(p2));
    try std.testing.expect(!p1.eql(p3));
}
