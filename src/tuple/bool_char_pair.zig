// Code generated by mapdb-codegen. DO NOT EDIT.

const std = @import("std");

/// Immutable pair of (`bool`, `u21`).
pub const BoolCharPair = struct {
    first: bool,
    second: u21,

    pub fn new(first: bool, second: u21) BoolCharPair {
        return .{ .first = first, .second = second };
    }

    pub fn one(self: BoolCharPair) bool {
        return self.first;
    }

    pub fn two(self: BoolCharPair) u21 {
        return self.second;
    }

    pub fn swap(self: BoolCharPair) CharBoolPair {
        return .{ .first = self.second, .second = self.first };
    }

    pub fn eql(self: BoolCharPair, other: BoolCharPair) bool {
        return (self.first == other.first) and (self.second == other.second);
    }

    pub fn format(self: BoolCharPair, comptime _: []const u8, _: std.fmt.FormatOptions, writer: anytype) !void {
        try std.fmt.format(writer, "({}, {})", .{ self.first, self.second });
    }
};

const CharBoolPair = @import("char_bool_pair.zig").CharBoolPair;

test "BoolCharPair: one and two" {
    const p = BoolCharPair.new(true, 'b');
    try std.testing.expectEqual(true, p.one());
    try std.testing.expectEqual('b', p.two());
}

test "BoolCharPair: eql" {
    const p1 = BoolCharPair.new(true, 'b');
    const p2 = BoolCharPair.new(true, 'b');
    const p3 = BoolCharPair.new(false, 'a');
    try std.testing.expect(p1.eql(p2));
    try std.testing.expect(!p1.eql(p3));
}
