// Code generated by mapdb-codegen. DO NOT EDIT.

const std = @import("std");
const Allocator = std.mem.Allocator;
const AllocatorConfig = @import("../allocator_config.zig").AllocatorConfig;

/// LIFO stack of `f32` values, backed by an ArrayList.
pub const F32ArrayStack = struct {
    items: std.ArrayListUnmanaged(f32) = .empty,
    config: AllocatorConfig,

    pub fn init(allocator: Allocator) F32ArrayStack {
        return .{ .config = AllocatorConfig.init(allocator) };
    }

    pub fn initWithConfig(config: AllocatorConfig) F32ArrayStack {
        return .{ .config = config };
    }

    pub fn deinit(self: *F32ArrayStack) void {
        self.items.deinit(self.config.itemsAllocator());
    }

    pub fn of(allocator: Allocator, values: []const f32) F32ArrayStack {
        var stack = init(allocator);
        stack.items.appendSlice(stack.config.itemsAllocator(), values) catch @panic("out of memory");
        return stack;
    }

    /// Pushes a value onto the top of the stack.
    pub fn push(self: *F32ArrayStack, value: f32) void {
        self.items.append(self.config.itemsAllocator(), value) catch @panic("out of memory");
    }

    /// Removes and returns the top element, or null if empty.
    pub fn pop(self: *F32ArrayStack) ?f32 {
        if (self.items.items.len == 0) return null;
        return self.items.pop();
    }

    /// Returns the top element without removing it, or null if empty.
    pub fn peek(self: *const F32ArrayStack) ?f32 {
        if (self.items.items.len == 0) return null;
        return self.items.items[self.items.items.len - 1];
    }

    /// Returns the element at the given depth from the top (0 = top).
    pub fn peekAt(self: *const F32ArrayStack, depth: usize) ?f32 {
        if (depth >= self.items.items.len) return null;
        return self.items.items[self.items.items.len - 1 - depth];
    }

    pub fn len(self: *const F32ArrayStack) usize {
        return self.items.items.len;
    }

    /// Alias for len() — matches Go/Java naming.
    pub fn size(self: *const F32ArrayStack) usize {
        return self.len();
    }

    pub fn isEmpty(self: *const F32ArrayStack) bool {
        return self.items.items.len == 0;
    }

    pub fn clear(self: *F32ArrayStack) void {
        self.items.clearRetainingCapacity();
    }

    // ---- Fallible capacity reservation ----

    /// Ensures that `additional` more items can be pushed without a
    /// reallocation. See `docs/zig/error-handling.md`.
    pub fn ensureUnusedCapacity(self: *F32ArrayStack, additional: usize) Allocator.Error!void {
        return self.items.ensureUnusedCapacity(self.config.itemsAllocator(), additional);
    }

    /// Ensures the stack's total capacity is at least `new_capacity`.
    pub fn ensureTotalCapacity(self: *F32ArrayStack, new_capacity: usize) Allocator.Error!void {
        return self.items.ensureTotalCapacity(self.config.itemsAllocator(), new_capacity);
    }

    pub fn contains(self: *const F32ArrayStack, value: f32) bool {
        for (self.items.items) |item| {
            if (@as(u32, @bitCast(item)) == @as(u32, @bitCast(value))) return true;
        }
        return false;
    }

    pub fn anySatisfy(self: *const F32ArrayStack, predicate: *const fn (f32) bool) bool {
        for (self.items.items) |item| {
            if (predicate(item)) return true;
        }
        return false;
    }

    pub fn allSatisfy(self: *const F32ArrayStack, predicate: *const fn (f32) bool) bool {
        for (self.items.items) |item| {
            if (!predicate(item)) return false;
        }
        return true;
    }

    // ---- Iteration ----

    /// Calls f for each element (bottom to top).
    pub fn forEach(self: *const F32ArrayStack, f: *const fn (f32) void) void {
        for (self.items.items) |item| f(item);
    }

    // ---- Functional Operations ----

    /// Returns a new stack with only elements satisfying the predicate.
    pub fn select(self: *const F32ArrayStack, predicate: *const fn (f32) bool) F32ArrayStack {
        var result = init(self.config.base);
        for (self.items.items) |item| {
            if (predicate(item)) result.push(item);
        }
        return result;
    }

    /// Returns a new stack with elements NOT satisfying the predicate.
    pub fn reject(self: *const F32ArrayStack, predicate: *const fn (f32) bool) F32ArrayStack {
        var result = init(self.config.base);
        for (self.items.items) |item| {
            if (!predicate(item)) result.push(item);
        }
        return result;
    }

    /// Returns the first element satisfying the predicate, or null.
    pub fn detect(self: *const F32ArrayStack, predicate: *const fn (f32) bool) ?f32 {
        for (self.items.items) |item| {
            if (predicate(item)) return item;
        }
        return null;
    }

    pub fn noneSatisfy(self: *const F32ArrayStack, predicate: *const fn (f32) bool) bool {
        for (self.items.items) |item| {
            if (predicate(item)) return false;
        }
        return true;
    }

    pub fn count(self: *const F32ArrayStack, predicate: *const fn (f32) bool) usize {
        var c: usize = 0;
        for (self.items.items) |item| {
            if (predicate(item)) c += 1;
        }
        return c;
    }

    pub fn injectInto(self: *const F32ArrayStack, initial: f32, f: *const fn (f32, f32) f32) f32 {
        var acc = initial;
        for (self.items.items) |item| acc = f(acc, item);
        return acc;
    }

    // ---- Conversion ----

    pub fn toSlice(self: *const F32ArrayStack) []const f32 {
        return self.items.items;
    }

    pub fn toImmutable(self: *const F32ArrayStack) @import("../immutable/immutable_f32_array_stack.zig").ImmutableF32ArrayStack {
        return @import("../immutable/immutable_f32_array_stack.zig").ImmutableF32ArrayStack.fromMutable(self.config.base, self);
    }

    // ---- Fluent API ----

    pub fn with(self: *F32ArrayStack, value: f32) *F32ArrayStack {
        self.push(value);
        return self;
    }

    pub fn withAll(self: *F32ArrayStack, values: []const f32) *F32ArrayStack {
        for (values) |val| self.push(val);
        return self;
    }

    // ---- Formatting ----

    /// Formats as "[top, ..., bottom]" (top-to-bottom order).
    pub fn format(self: *const F32ArrayStack, comptime _: []const u8, _: std.fmt.FormatOptions, writer: anytype) !void {
        try writer.writeAll("[");
        var i = self.items.items.len;
        var first = true;
        while (i > 0) {
            i -= 1;
            if (!first) try writer.writeAll(", ");
            try std.fmt.formatType(self.items.items[i], "", .{}, writer, 0);
            first = false;
        }
        try writer.writeAll("]");
    }

    // ---- Equality ----

    pub fn eql(self: *const F32ArrayStack, other: *const F32ArrayStack) bool {
        if (self.items.items.len != other.items.items.len) return false;
        for (self.items.items, other.items.items) |a, b| {
            if (!(@as(u32, @bitCast(a)) == @as(u32, @bitCast(b)))) return false;
        }
        return true;
    }
};

test "F32ArrayStack: push peek pop" {
    var stack = F32ArrayStack.init(std.testing.allocator);
    defer stack.deinit();
    stack.push(1.0);
    stack.push(2.0);
    stack.push(3.0);
    try std.testing.expectEqual(@as(usize, 3), stack.len());
    try std.testing.expectEqual(@as(?f32, 3.0), stack.peek());
    try std.testing.expectEqual(@as(?f32, 3.0), stack.pop());
    try std.testing.expectEqual(@as(usize, 2), stack.len());
}

test "F32ArrayStack: pop empty" {
    var stack = F32ArrayStack.init(std.testing.allocator);
    defer stack.deinit();
    try std.testing.expectEqual(@as(?f32, null), stack.pop());
    try std.testing.expectEqual(@as(?f32, null), stack.peek());
}

test "F32ArrayStack: LIFO order" {
    var stack = F32ArrayStack.init(std.testing.allocator);
    defer stack.deinit();
    stack.push(1.0);
    stack.push(2.0);
    stack.push(3.0);
    try std.testing.expectEqual(@as(?f32, 3.0), stack.pop());
    try std.testing.expectEqual(@as(?f32, 2.0), stack.pop());
    try std.testing.expectEqual(@as(?f32, 1.0), stack.pop());
}

test "F32ArrayStack: contains" {
    var stack = F32ArrayStack.init(std.testing.allocator);
    defer stack.deinit();
    stack.push(1.0);
    try std.testing.expect(stack.contains(1.0));
    try std.testing.expect(!stack.contains(3.0));
}

test "F32ArrayStack: clear" {
    var stack = F32ArrayStack.of(std.testing.allocator, &[_]f32{1.0});
    defer stack.deinit();
    stack.clear();
    try std.testing.expect(stack.isEmpty());
}

test "F32ArrayStack: eql" {
    var s1 = F32ArrayStack.of(std.testing.allocator, &[_]f32{ 1.0, 2.0 });
    defer s1.deinit();
    var s2 = F32ArrayStack.of(std.testing.allocator, &[_]f32{ 1.0, 2.0 });
    defer s2.deinit();
    var s3 = F32ArrayStack.of(std.testing.allocator, &[_]f32{1.0});
    defer s3.deinit();
    try std.testing.expect(s1.eql(&s2));
    try std.testing.expect(!s1.eql(&s3));
}

test "F32ArrayStack: select and reject" {
    var stack = F32ArrayStack.of(std.testing.allocator, &[_]f32{ 1.0, 2.0, 3.0 });
    defer stack.deinit();
    var sel = stack.select(struct {
        fn f(val: f32) bool {
            return val > 1.0;
        }
    }.f);
    defer sel.deinit();
    try std.testing.expectEqual(@as(usize, 2), sel.len());
}

test "F32ArrayStack: toImmutable" {
    var stack = F32ArrayStack.of(std.testing.allocator, &[_]f32{ 1.0, 2.0 });
    defer stack.deinit();
    var imm = stack.toImmutable();
    defer imm.deinit();
    try std.testing.expectEqual(@as(usize, 2), imm.len());
    stack.push(3.0);
    try std.testing.expectEqual(@as(usize, 2), imm.len());
}

test "F32ArrayStack: fluent with" {
    var stack = F32ArrayStack.init(std.testing.allocator);
    defer stack.deinit();
    _ = stack.with(1.0).with(2.0);
    try std.testing.expectEqual(@as(usize, 2), stack.len());
}

test "F32ArrayStack: ensureUnusedCapacity reserves and subsequent push does not reallocate" {
    var stack = F32ArrayStack.init(std.testing.allocator);
    defer stack.deinit();
    try stack.ensureUnusedCapacity(10);
    const reserved = stack.items.capacity;
    try std.testing.expect(reserved >= 10);
    stack.push(1.0);
    stack.push(2.0);
    stack.push(3.0);
    try std.testing.expectEqual(reserved, stack.items.capacity);
}

test "F32ArrayStack: ensureTotalCapacity sets minimum capacity" {
    var stack = F32ArrayStack.init(std.testing.allocator);
    defer stack.deinit();
    try stack.ensureTotalCapacity(64);
    try std.testing.expect(stack.items.capacity >= 64);
}

test "F32ArrayStack: ensureUnusedCapacity propagates allocator error" {
    // fail_index = 0: Stack.init doesn't allocate (backed by ArrayList),
    // so the first alloc (ensureUnusedCapacity grow) fails immediately.
    var failing = std.testing.FailingAllocator.init(std.testing.allocator, .{ .fail_index = 0 });
    var stack = F32ArrayStack.initWithConfig(AllocatorConfig.init(failing.allocator()));
    defer stack.deinit();
    try std.testing.expectError(error.OutOfMemory, stack.ensureUnusedCapacity(1024));
}
