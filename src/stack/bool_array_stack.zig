// Code generated by mapdb-codegen. DO NOT EDIT.

const std = @import("std");
const Allocator = std.mem.Allocator;
const AllocatorConfig = @import("../allocator_config.zig").AllocatorConfig;

/// LIFO stack of `bool` values, backed by an ArrayList.
pub const BoolArrayStack = struct {
    items: std.ArrayListUnmanaged(bool) = .empty,
    config: AllocatorConfig,

    pub fn init(allocator: Allocator) BoolArrayStack {
        return .{ .config = AllocatorConfig.init(allocator) };
    }

    pub fn initWithConfig(config: AllocatorConfig) BoolArrayStack {
        return .{ .config = config };
    }

    pub fn deinit(self: *BoolArrayStack) void {
        self.items.deinit(self.config.itemsAllocator());
    }

    pub fn of(allocator: Allocator, values: []const bool) BoolArrayStack {
        var stack = init(allocator);
        stack.items.appendSlice(stack.config.itemsAllocator(), values) catch @panic("out of memory");
        return stack;
    }

    /// Pushes a value onto the top of the stack.
    pub fn push(self: *BoolArrayStack, value: bool) void {
        self.items.append(self.config.itemsAllocator(), value) catch @panic("out of memory");
    }

    /// Removes and returns the top element, or null if empty.
    pub fn pop(self: *BoolArrayStack) ?bool {
        if (self.items.items.len == 0) return null;
        return self.items.pop();
    }

    /// Returns the top element without removing it, or null if empty.
    pub fn peek(self: *const BoolArrayStack) ?bool {
        if (self.items.items.len == 0) return null;
        return self.items.items[self.items.items.len - 1];
    }

    /// Returns the element at the given depth from the top (0 = top).
    pub fn peekAt(self: *const BoolArrayStack, depth: usize) ?bool {
        if (depth >= self.items.items.len) return null;
        return self.items.items[self.items.items.len - 1 - depth];
    }

    pub fn len(self: *const BoolArrayStack) usize {
        return self.items.items.len;
    }

    /// Alias for len() — matches Go/Java naming.
    pub fn size(self: *const BoolArrayStack) usize {
        return self.len();
    }

    pub fn isEmpty(self: *const BoolArrayStack) bool {
        return self.items.items.len == 0;
    }

    pub fn clear(self: *BoolArrayStack) void {
        self.items.clearRetainingCapacity();
    }

    // ---- Fallible capacity reservation ----

    /// Ensures that `additional` more items can be pushed without a
    /// reallocation. See `docs/zig/error-handling.md`.
    pub fn ensureUnusedCapacity(self: *BoolArrayStack, additional: usize) Allocator.Error!void {
        return self.items.ensureUnusedCapacity(self.config.itemsAllocator(), additional);
    }

    /// Ensures the stack's total capacity is at least `new_capacity`.
    pub fn ensureTotalCapacity(self: *BoolArrayStack, new_capacity: usize) Allocator.Error!void {
        return self.items.ensureTotalCapacity(self.config.itemsAllocator(), new_capacity);
    }

    pub fn contains(self: *const BoolArrayStack, value: bool) bool {
        for (self.items.items) |item| {
            if (item == value) return true;
        }
        return false;
    }

    pub fn anySatisfy(self: *const BoolArrayStack, predicate: *const fn (bool) bool) bool {
        for (self.items.items) |item| {
            if (predicate(item)) return true;
        }
        return false;
    }

    pub fn allSatisfy(self: *const BoolArrayStack, predicate: *const fn (bool) bool) bool {
        for (self.items.items) |item| {
            if (!predicate(item)) return false;
        }
        return true;
    }

    // ---- Iteration ----

    /// Calls f for each element (bottom to top).
    pub fn forEach(self: *const BoolArrayStack, f: *const fn (bool) void) void {
        for (self.items.items) |item| f(item);
    }

    // ---- Functional Operations ----

    /// Returns a new stack with only elements satisfying the predicate.
    pub fn select(self: *const BoolArrayStack, predicate: *const fn (bool) bool) BoolArrayStack {
        var result = init(self.config.base);
        for (self.items.items) |item| {
            if (predicate(item)) result.push(item);
        }
        return result;
    }

    /// Returns a new stack with elements NOT satisfying the predicate.
    pub fn reject(self: *const BoolArrayStack, predicate: *const fn (bool) bool) BoolArrayStack {
        var result = init(self.config.base);
        for (self.items.items) |item| {
            if (!predicate(item)) result.push(item);
        }
        return result;
    }

    /// Returns the first element satisfying the predicate, or null.
    pub fn detect(self: *const BoolArrayStack, predicate: *const fn (bool) bool) ?bool {
        for (self.items.items) |item| {
            if (predicate(item)) return item;
        }
        return null;
    }

    pub fn noneSatisfy(self: *const BoolArrayStack, predicate: *const fn (bool) bool) bool {
        for (self.items.items) |item| {
            if (predicate(item)) return false;
        }
        return true;
    }

    pub fn count(self: *const BoolArrayStack, predicate: *const fn (bool) bool) usize {
        var c: usize = 0;
        for (self.items.items) |item| {
            if (predicate(item)) c += 1;
        }
        return c;
    }

    pub fn injectInto(self: *const BoolArrayStack, initial: bool, f: *const fn (bool, bool) bool) bool {
        var acc = initial;
        for (self.items.items) |item| acc = f(acc, item);
        return acc;
    }

    // ---- Conversion ----

    pub fn toSlice(self: *const BoolArrayStack) []const bool {
        return self.items.items;
    }

    pub fn toImmutable(self: *const BoolArrayStack) @import("../immutable/immutable_bool_array_stack.zig").ImmutableBoolArrayStack {
        return @import("../immutable/immutable_bool_array_stack.zig").ImmutableBoolArrayStack.fromMutable(self.config.base, self);
    }

    // ---- Fluent API ----

    pub fn with(self: *BoolArrayStack, value: bool) *BoolArrayStack {
        self.push(value);
        return self;
    }

    pub fn withAll(self: *BoolArrayStack, values: []const bool) *BoolArrayStack {
        for (values) |val| self.push(val);
        return self;
    }

    // ---- Formatting ----

    /// Formats as "[top, ..., bottom]" (top-to-bottom order).
    pub fn format(self: *const BoolArrayStack, comptime _: []const u8, _: std.fmt.FormatOptions, writer: anytype) !void {
        try writer.writeAll("[");
        var i = self.items.items.len;
        var first = true;
        while (i > 0) {
            i -= 1;
            if (!first) try writer.writeAll(", ");
            try std.fmt.formatType(self.items.items[i], "", .{}, writer, 0);
            first = false;
        }
        try writer.writeAll("]");
    }

    // ---- Equality ----

    pub fn eql(self: *const BoolArrayStack, other: *const BoolArrayStack) bool {
        if (self.items.items.len != other.items.items.len) return false;
        for (self.items.items, other.items.items) |a, b| {
            if (!(a == b)) return false;
        }
        return true;
    }
};

test "BoolArrayStack: push peek pop" {
    var stack = BoolArrayStack.init(std.testing.allocator);
    defer stack.deinit();
    stack.push(true);
    stack.push(false);
    stack.push(true);
    try std.testing.expectEqual(@as(usize, 3), stack.len());
    try std.testing.expectEqual(@as(?bool, true), stack.peek());
    try std.testing.expectEqual(@as(?bool, true), stack.pop());
    try std.testing.expectEqual(@as(usize, 2), stack.len());
}

test "BoolArrayStack: pop empty" {
    var stack = BoolArrayStack.init(std.testing.allocator);
    defer stack.deinit();
    try std.testing.expectEqual(@as(?bool, null), stack.pop());
    try std.testing.expectEqual(@as(?bool, null), stack.peek());
}

test "BoolArrayStack: LIFO order" {
    var stack = BoolArrayStack.init(std.testing.allocator);
    defer stack.deinit();
    stack.push(true);
    stack.push(false);
    stack.push(true);
    try std.testing.expectEqual(@as(?bool, true), stack.pop());
    try std.testing.expectEqual(@as(?bool, false), stack.pop());
    try std.testing.expectEqual(@as(?bool, true), stack.pop());
}

test "BoolArrayStack: contains" {
    var stack = BoolArrayStack.init(std.testing.allocator);
    defer stack.deinit();
    stack.push(true);
    try std.testing.expect(stack.contains(true));
}

test "BoolArrayStack: clear" {
    var stack = BoolArrayStack.of(std.testing.allocator, &[_]bool{true});
    defer stack.deinit();
    stack.clear();
    try std.testing.expect(stack.isEmpty());
}

test "BoolArrayStack: eql" {
    var s1 = BoolArrayStack.of(std.testing.allocator, &[_]bool{ true, false });
    defer s1.deinit();
    var s2 = BoolArrayStack.of(std.testing.allocator, &[_]bool{ true, false });
    defer s2.deinit();
    var s3 = BoolArrayStack.of(std.testing.allocator, &[_]bool{true});
    defer s3.deinit();
    try std.testing.expect(s1.eql(&s2));
    try std.testing.expect(!s1.eql(&s3));
}

test "BoolArrayStack: select" {
    var stack = BoolArrayStack.of(std.testing.allocator, &[_]bool{ true, false, true });
    defer stack.deinit();
    var sel = stack.select(struct {
        fn f(val: bool) bool {
            return val;
        }
    }.f);
    defer sel.deinit();
    try std.testing.expectEqual(@as(usize, 2), sel.len());
}

test "BoolArrayStack: toImmutable" {
    var stack = BoolArrayStack.of(std.testing.allocator, &[_]bool{ true, false });
    defer stack.deinit();
    var imm = stack.toImmutable();
    defer imm.deinit();
    try std.testing.expectEqual(@as(usize, 2), imm.len());
    stack.push(true);
    try std.testing.expectEqual(@as(usize, 2), imm.len());
}

test "BoolArrayStack: fluent with" {
    var stack = BoolArrayStack.init(std.testing.allocator);
    defer stack.deinit();
    _ = stack.with(true).with(false);
    try std.testing.expectEqual(@as(usize, 2), stack.len());
}

test "BoolArrayStack: ensureUnusedCapacity reserves and subsequent push does not reallocate" {
    var stack = BoolArrayStack.init(std.testing.allocator);
    defer stack.deinit();
    try stack.ensureUnusedCapacity(10);
    const reserved = stack.items.capacity;
    try std.testing.expect(reserved >= 10);
    stack.push(true);
    stack.push(false);
    stack.push(true);
    try std.testing.expectEqual(reserved, stack.items.capacity);
}

test "BoolArrayStack: ensureTotalCapacity sets minimum capacity" {
    var stack = BoolArrayStack.init(std.testing.allocator);
    defer stack.deinit();
    try stack.ensureTotalCapacity(64);
    try std.testing.expect(stack.items.capacity >= 64);
}

test "BoolArrayStack: ensureUnusedCapacity propagates allocator error" {
    // fail_index = 0: Stack.init doesn't allocate (backed by ArrayList),
    // so the first alloc (ensureUnusedCapacity grow) fails immediately.
    var failing = std.testing.FailingAllocator.init(std.testing.allocator, .{ .fail_index = 0 });
    var stack = BoolArrayStack.initWithConfig(AllocatorConfig.init(failing.allocator()));
    defer stack.deinit();
    try std.testing.expectError(error.OutOfMemory, stack.ensureUnusedCapacity(1024));
}
