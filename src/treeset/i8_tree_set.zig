// Code generated by mapdb-codegen. DO NOT EDIT.

const std = @import("std");
const Allocator = std.mem.Allocator;
const AllocatorConfig = @import("../allocator_config.zig").AllocatorConfig;

/// Sorted set of unique `i8` values, backed by `std.Treap` for
/// O(log n) insert, remove, and lookup.
pub const I8TreeSet = struct {
    fn orderFn(a: i8, b: i8) std.math.Order {
        return std.math.order(a, b);
    }

    const TreapType = std.Treap(i8, orderFn);

    treap: TreapType = .{},
    config: AllocatorConfig,
    node_count: usize = 0,

    pub fn init(allocator: Allocator) I8TreeSet {
        return .{ .config = AllocatorConfig.init(allocator) };
    }

    pub fn initWithConfig(config: AllocatorConfig) I8TreeSet {
        return .{ .config = config };
    }

    pub fn deinit(self: *I8TreeSet) void {
        destroySubtree(self.treap.root, self.config.keysAllocator());
    }

    fn destroySubtree(node_opt: ?*TreapType.Node, allocator: Allocator) void {
        const node = node_opt orelse return;
        destroySubtree(node.children[0], allocator);
        destroySubtree(node.children[1], allocator);
        allocator.destroy(node);
    }

    pub fn of(allocator: Allocator, values: []const i8) I8TreeSet {
        var set = init(allocator);
        for (values) |val| {
            _ = set.add(val);
        }
        return set;
    }

    /// Adds a value. Returns true if it was not already present.
    pub fn add(self: *I8TreeSet, value: i8) bool {
        var entry = self.treap.getEntryFor(value);
        if (entry.node != null) return false;
        const node = self.config.keysAllocator().create(TreapType.Node) catch @panic("out of memory");
        entry.set(node);
        self.node_count += 1;
        return true;
    }

    /// Removes a value. Returns true if it was present.
    pub fn remove(self: *I8TreeSet, value: i8) bool {
        var entry = self.treap.getEntryFor(value);
        const node = entry.node orelse return false;
        entry.set(null);
        self.config.keysAllocator().destroy(node);
        self.node_count -= 1;
        return true;
    }

    pub fn contains(self: *const I8TreeSet, value: i8) bool {
        var node = self.treap.root;
        while (node) |current| {
            const ord = orderFn(value, current.key);
            if (ord == .eq) return true;
            node = current.children[@intFromBool(ord == .gt)];
        }
        return false;
    }

    pub fn len(self: *const I8TreeSet) usize {
        return self.node_count;
    }

    /// Alias for len() — matches Go/Java naming.
    pub fn size(self: *const I8TreeSet) usize {
        return self.node_count;
    }

    pub fn isEmpty(self: *const I8TreeSet) bool {
        return self.node_count == 0;
    }

    pub fn clear(self: *I8TreeSet) void {
        destroySubtree(self.treap.root, self.config.keysAllocator());
        self.treap.root = null;
        self.node_count = 0;
    }

    // ---- Fallible capacity reservation ----

    /// Probes that the allocator can serve `additional` node allocations.
    /// Returns `error.OutOfMemory` if the allocator fails.
    pub fn ensureUnusedCapacity(self: *I8TreeSet, additional: usize) Allocator.Error!void {
        const probe = try self.config.keysAllocator().alloc(TreapType.Node, additional);
        self.config.keysAllocator().free(probe);
    }

    /// Probes that the allocator can serve enough nodes for `new_capacity`
    /// total elements.
    pub fn ensureTotalCapacity(self: *I8TreeSet, new_capacity: usize) Allocator.Error!void {
        if (new_capacity <= self.node_count) return;
        try self.ensureUnusedCapacity(new_capacity - self.node_count);
    }

    pub fn min(self: *const I8TreeSet) ?i8 {
        const node = self.treap.getMin() orelse return null;
        return node.key;
    }

    pub fn max(self: *const I8TreeSet) ?i8 {
        const node = self.treap.getMax() orelse return null;
        return node.key;
    }

    /// Returns all values in sorted order. Caller must free the returned slice.
    pub fn toSlice(self: *const I8TreeSet, allocator: Allocator) []i8 {
        var result = std.ArrayListUnmanaged(i8){};
        result.ensureTotalCapacity(allocator, self.node_count) catch @panic("out of memory");
        var it = TreapType.InorderIterator{ .current = self.treap.getMin() };
        while (it.next()) |node| {
            result.appendAssumeCapacity(node.key);
        }
        return result.toOwnedSlice(allocator) catch @panic("out of memory");
    }

    // ---- Iteration ----

    pub fn forEach(self: *const I8TreeSet, f: *const fn (i8) void) void {
        var it = TreapType.InorderIterator{ .current = self.treap.getMin() };
        while (it.next()) |node| f(node.key);
    }

    // ---- Functional Operations ----

    pub fn select(self: *const I8TreeSet, predicate: *const fn (i8) bool) I8TreeSet {
        var result = init(self.config.base);
        var it = TreapType.InorderIterator{ .current = self.treap.getMin() };
        while (it.next()) |node| {
            if (predicate(node.key)) _ = result.add(node.key);
        }
        return result;
    }

    pub fn reject(self: *const I8TreeSet, predicate: *const fn (i8) bool) I8TreeSet {
        var result = init(self.config.base);
        var it = TreapType.InorderIterator{ .current = self.treap.getMin() };
        while (it.next()) |node| {
            if (!predicate(node.key)) _ = result.add(node.key);
        }
        return result;
    }

    pub fn detect(self: *const I8TreeSet, predicate: *const fn (i8) bool) ?i8 {
        var it = TreapType.InorderIterator{ .current = self.treap.getMin() };
        while (it.next()) |node| {
            if (predicate(node.key)) return node.key;
        }
        return null;
    }

    pub fn anySatisfy(self: *const I8TreeSet, predicate: *const fn (i8) bool) bool {
        var it = TreapType.InorderIterator{ .current = self.treap.getMin() };
        while (it.next()) |node| {
            if (predicate(node.key)) return true;
        }
        return false;
    }

    pub fn allSatisfy(self: *const I8TreeSet, predicate: *const fn (i8) bool) bool {
        var it = TreapType.InorderIterator{ .current = self.treap.getMin() };
        while (it.next()) |node| {
            if (!predicate(node.key)) return false;
        }
        return true;
    }

    pub fn noneSatisfy(self: *const I8TreeSet, predicate: *const fn (i8) bool) bool {
        var it = TreapType.InorderIterator{ .current = self.treap.getMin() };
        while (it.next()) |node| {
            if (predicate(node.key)) return false;
        }
        return true;
    }

    pub fn count(self: *const I8TreeSet, predicate: *const fn (i8) bool) usize {
        var c: usize = 0;
        var it = TreapType.InorderIterator{ .current = self.treap.getMin() };
        while (it.next()) |node| {
            if (predicate(node.key)) c += 1;
        }
        return c;
    }

    // ---- Set Operations ----

    pub fn setUnion(self: *const I8TreeSet, other: *const I8TreeSet) I8TreeSet {
        var result = init(self.config.base);
        var it1 = TreapType.InorderIterator{ .current = self.treap.getMin() };
        while (it1.next()) |node| _ = result.add(node.key);
        var it2 = TreapType.InorderIterator{ .current = other.treap.getMin() };
        while (it2.next()) |node| _ = result.add(node.key);
        return result;
    }

    pub fn intersect(self: *const I8TreeSet, other: *const I8TreeSet) I8TreeSet {
        var result = init(self.config.base);
        var it = TreapType.InorderIterator{ .current = self.treap.getMin() };
        while (it.next()) |node| {
            if (other.contains(node.key)) _ = result.add(node.key);
        }
        return result;
    }

    pub fn difference(self: *const I8TreeSet, other: *const I8TreeSet) I8TreeSet {
        var result = init(self.config.base);
        var it = TreapType.InorderIterator{ .current = self.treap.getMin() };
        while (it.next()) |node| {
            if (!other.contains(node.key)) _ = result.add(node.key);
        }
        return result;
    }

    // ---- Range Operations ----

    /// Returns the node for the smallest element >= value, or null.
    fn ceilingNode(self: *const I8TreeSet, value: i8) ?*TreapType.Node {
        var node = self.treap.root;
        var best: ?*TreapType.Node = null;
        while (node) |current| {
            const ord = orderFn(value, current.key);
            if (ord == .eq) return current;
            if (ord == .lt) {
                best = current;
                node = current.children[0];
            } else {
                node = current.children[1];
            }
        }
        return best;
    }

    /// Returns the smallest element >= value, or null if none.
    pub fn ceiling(self: *const I8TreeSet, value: i8) ?i8 {
        const node = self.ceilingNode(value) orelse return null;
        return node.key;
    }

    /// Returns the largest element <= value, or null if none.
    pub fn floor(self: *const I8TreeSet, value: i8) ?i8 {
        var node = self.treap.root;
        var best: ?i8 = null;
        while (node) |current| {
            const ord = orderFn(value, current.key);
            if (ord == .eq) return current.key;
            if (ord == .gt) {
                best = current.key;
                node = current.children[1];
            } else {
                node = current.children[0];
            }
        }
        return best;
    }

    /// Returns all elements in [from, to] inclusive. Caller must free.
    pub fn rangeValues(self: *const I8TreeSet, from: i8, to: i8, allocator: Allocator) []i8 {
        var result = std.ArrayListUnmanaged(i8){};
        var it = TreapType.InorderIterator{ .current = self.ceilingNode(from) };
        while (it.next()) |node| {
            if (orderFn(node.key, to) == .gt) break;
            result.append(allocator, node.key) catch @panic("out of memory");
        }
        return result.toOwnedSlice(allocator) catch @panic("out of memory");
    }

    // ---- Formatting ----

    /// Formats as "{v1, v2, v3}" in sorted order.
    pub fn format(self: *const I8TreeSet, comptime _: []const u8, _: std.fmt.FormatOptions, writer: anytype) !void {
        try writer.writeAll("{");
        var first = true;
        var it = TreapType.InorderIterator{ .current = self.treap.getMin() };
        while (it.next()) |node| {
            if (!first) try writer.writeAll(", ");
            try std.fmt.formatType(node.key, "", .{}, writer, 0);
            first = false;
        }
        try writer.writeAll("}");
    }

    // ---- Fluent API ----

    pub fn with(self: *I8TreeSet, value: i8) *I8TreeSet {
        _ = self.add(value);
        return self;
    }

    pub fn without(self: *I8TreeSet, value: i8) *I8TreeSet {
        _ = self.remove(value);
        return self;
    }

    // ---- Equality ----

    pub fn eql(self: *const I8TreeSet, other: *const I8TreeSet) bool {
        if (self.node_count != other.node_count) return false;
        var it1 = TreapType.InorderIterator{ .current = self.treap.getMin() };
        var it2 = TreapType.InorderIterator{ .current = other.treap.getMin() };
        while (it1.next()) |n1| {
            const n2 = it2.next() orelse return false;
            if (!(n1.key == n2.key)) return false;
        }
        return true;
    }
};

test "I8TreeSet: add and contains" {
    var set = I8TreeSet.init(std.testing.allocator);
    defer set.deinit();
    _ = set.add(1);
    _ = set.add(2);
    _ = set.add(3);
    try std.testing.expectEqual(@as(usize, 3), set.len());
    try std.testing.expect(set.contains(2));
    try std.testing.expect(!set.contains(99));
}

test "I8TreeSet: duplicate" {
    var set = I8TreeSet.init(std.testing.allocator);
    defer set.deinit();
    try std.testing.expect(set.add(1));
    try std.testing.expect(!set.add(1));
    try std.testing.expectEqual(@as(usize, 1), set.len());
}

test "I8TreeSet: sorted iteration" {
    var set = I8TreeSet.of(std.testing.allocator, &[_]i8{ 3, 1, 2 });
    defer set.deinit();
    const items = set.toSlice(std.testing.allocator);
    defer std.testing.allocator.free(items);
    try std.testing.expectEqual(1, items[0]);
    try std.testing.expectEqual(2, items[1]);
    try std.testing.expectEqual(3, items[2]);
}

test "I8TreeSet: min max" {
    var set = I8TreeSet.of(std.testing.allocator, &[_]i8{ 3, 1, 2 });
    defer set.deinit();
    try std.testing.expectEqual(@as(?i8, 1), set.min());
    try std.testing.expectEqual(@as(?i8, 3), set.max());
}

test "I8TreeSet: remove" {
    var set = I8TreeSet.of(std.testing.allocator, &[_]i8{ 1, 2 });
    defer set.deinit();
    try std.testing.expect(set.remove(1));
    try std.testing.expect(!set.contains(1));
}

test "I8TreeSet: union" {
    var a = I8TreeSet.of(std.testing.allocator, &[_]i8{ 1, 2 });
    defer a.deinit();
    var b = I8TreeSet.of(std.testing.allocator, &[_]i8{ 2, 3 });
    defer b.deinit();
    var u = a.setUnion(&b);
    defer u.deinit();
    try std.testing.expectEqual(@as(usize, 3), u.len());
}

test "I8TreeSet: intersect" {
    var a = I8TreeSet.of(std.testing.allocator, &[_]i8{ 1, 2 });
    defer a.deinit();
    var b = I8TreeSet.of(std.testing.allocator, &[_]i8{ 2, 3 });
    defer b.deinit();
    var inter = a.intersect(&b);
    defer inter.deinit();
    try std.testing.expectEqual(@as(usize, 1), inter.len());
}

test "I8TreeSet: ensureUnusedCapacity probes allocator" {
    var set = I8TreeSet.init(std.testing.allocator);
    defer set.deinit();
    try set.ensureUnusedCapacity(100);
    _ = set.add(1);
    _ = set.add(2);
    try std.testing.expectEqual(@as(usize, 2), set.len());
}

test "I8TreeSet: ensureUnusedCapacity propagates allocator error" {
    // fail_index = 0: TreeSet.init doesn't allocate (Treap is node-based),
    // so the first alloc (ensureUnusedCapacity grow) fails immediately.
    var failing = std.testing.FailingAllocator.init(std.testing.allocator, .{ .fail_index = 0 });
    var set = I8TreeSet.init(failing.allocator());
    defer set.deinit();
    try std.testing.expectError(error.OutOfMemory, set.ensureUnusedCapacity(1024));
}
