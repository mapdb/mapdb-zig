// Code generated by mapdb-codegen. DO NOT EDIT.

const std = @import("std");
const Allocator = std.mem.Allocator;
const OpenHashMap = @import("../hash_table.zig").OpenHashMap;
const I16BoolHashMap = @import("../hashmap/i16_bool_hash_map.zig").I16BoolHashMap;

/// Immutable hash map from `i16` keys to `bool` values.
///
/// Backed by a snapshot of the internal hash map. All operations are read-only.
pub const ImmutableI16BoolHashMap = struct {
    inner: OpenHashMap(i16, bool),
    allocator: Allocator,

    pub fn fromMutable(allocator: Allocator, mutable: *const I16BoolHashMap) ImmutableI16BoolHashMap {
        var inner = OpenHashMap(i16, bool).init(allocator, allocator, allocator) catch @panic("out of memory");
        for (0..mutable.inner.capacity) |i| {
            if (mutable.inner.entries[i].occupied) {
                _ = inner.put(mutable.inner.entries[i].key, mutable.inner.entries[i].value) catch @panic("out of memory");
            }
        }
        return .{ .inner = inner, .allocator = allocator };
    }

    pub fn deinit(self: *ImmutableI16BoolHashMap) void {
        self.inner.deinit();
    }

    pub fn get(self: *const ImmutableI16BoolHashMap, key: i16) ?bool {
        return self.inner.get(key);
    }

    pub fn getOrDefault(self: *const ImmutableI16BoolHashMap, key: i16, default_value: bool) bool {
        return self.get(key) orelse default_value;
    }

    pub fn containsKey(self: *const ImmutableI16BoolHashMap, key: i16) bool {
        return self.inner.containsKey(key);
    }

    pub fn containsValue(self: *const ImmutableI16BoolHashMap, value: bool) bool {
        return self.inner.containsValue(value);
    }

    pub fn len(self: *const ImmutableI16BoolHashMap) usize {
        return self.inner.len();
    }

    pub fn isEmpty(self: *const ImmutableI16BoolHashMap) bool {
        return self.inner.isEmpty();
    }

    pub fn toMutable(self: *const ImmutableI16BoolHashMap) I16BoolHashMap {
        var mutable = I16BoolHashMap.init(self.allocator);
        for (0..self.inner.capacity) |i| {
            if (self.inner.entries[i].occupied) {
                _ = mutable.put(self.inner.entries[i].key, self.inner.entries[i].value);
            }
        }
        return mutable;
    }

    pub fn eql(self: *const ImmutableI16BoolHashMap, other: *const ImmutableI16BoolHashMap) bool {
        if (self.len() != other.len()) return false;
        for (0..self.inner.capacity) |i| {
            if (self.inner.entries[i].occupied) {
                const other_val = other.get(self.inner.entries[i].key) orelse return false;
                if (!(self.inner.entries[i].value == other_val)) return false;
            }
        }
        return true;
    }
};

test "ImmutableI16BoolHashMap: fromMutable and get" {
    var mm = I16BoolHashMap.init(std.testing.allocator);
    defer mm.deinit();
    _ = mm.put(1, true);
    _ = mm.put(2, false);
    var im = ImmutableI16BoolHashMap.fromMutable(std.testing.allocator, &mm);
    defer im.deinit();
    try std.testing.expectEqual(@as(?bool, true), im.get(1));
    try std.testing.expectEqual(@as(usize, 2), im.len());
}

test "ImmutableI16BoolHashMap: toMutable independence" {
    var mm = I16BoolHashMap.init(std.testing.allocator);
    _ = mm.put(1, true);
    var im = ImmutableI16BoolHashMap.fromMutable(std.testing.allocator, &mm);
    mm.deinit();
    defer im.deinit();
    var mm2 = im.toMutable();
    defer mm2.deinit();
    _ = mm2.put(2, false);
    try std.testing.expectEqual(@as(usize, 1), im.len());
    try std.testing.expectEqual(@as(usize, 2), mm2.len());
}
