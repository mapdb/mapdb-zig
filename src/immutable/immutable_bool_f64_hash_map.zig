// Code generated by mapdb-codegen. DO NOT EDIT.

const std = @import("std");
const Allocator = std.mem.Allocator;
const OpenHashMap = @import("../hash_table.zig").OpenHashMap;
const BoolF64HashMap = @import("../hashmap/bool_f64_hash_map.zig").BoolF64HashMap;

/// Immutable hash map from `bool` keys to `f64` values.
///
/// Backed by a snapshot of the internal hash map. All operations are read-only.
pub const ImmutableBoolF64HashMap = struct {
    inner: OpenHashMap(bool, f64),
    allocator: Allocator,

    pub fn fromMutable(allocator: Allocator, mutable: *const BoolF64HashMap) ImmutableBoolF64HashMap {
        var inner = OpenHashMap(bool, f64).init(allocator, allocator, allocator) catch @panic("out of memory");
        for (0..mutable.inner.capacity) |i| {
            if (mutable.inner.entries[i].occupied) {
                _ = inner.put(mutable.inner.entries[i].key, mutable.inner.entries[i].value) catch @panic("out of memory");
            }
        }
        return .{ .inner = inner, .allocator = allocator };
    }

    pub fn deinit(self: *ImmutableBoolF64HashMap) void {
        self.inner.deinit();
    }

    pub fn get(self: *const ImmutableBoolF64HashMap, key: bool) ?f64 {
        return self.inner.get(key);
    }

    pub fn getOrDefault(self: *const ImmutableBoolF64HashMap, key: bool, default_value: f64) f64 {
        return self.get(key) orelse default_value;
    }

    pub fn containsKey(self: *const ImmutableBoolF64HashMap, key: bool) bool {
        return self.inner.containsKey(key);
    }

    pub fn containsValue(self: *const ImmutableBoolF64HashMap, value: f64) bool {
        return self.inner.containsValue(value);
    }

    pub fn len(self: *const ImmutableBoolF64HashMap) usize {
        return self.inner.len();
    }

    pub fn isEmpty(self: *const ImmutableBoolF64HashMap) bool {
        return self.inner.isEmpty();
    }

    pub fn toMutable(self: *const ImmutableBoolF64HashMap) BoolF64HashMap {
        var mutable = BoolF64HashMap.init(self.allocator);
        for (0..self.inner.capacity) |i| {
            if (self.inner.entries[i].occupied) {
                _ = mutable.put(self.inner.entries[i].key, self.inner.entries[i].value);
            }
        }
        return mutable;
    }

    pub fn eql(self: *const ImmutableBoolF64HashMap, other: *const ImmutableBoolF64HashMap) bool {
        if (self.len() != other.len()) return false;
        for (0..self.inner.capacity) |i| {
            if (self.inner.entries[i].occupied) {
                const other_val = other.get(self.inner.entries[i].key) orelse return false;
                if (!(@as(u64, @bitCast(self.inner.entries[i].value)) == @as(u64, @bitCast(other_val)))) return false;
            }
        }
        return true;
    }
};

test "ImmutableBoolF64HashMap: fromMutable and get" {
    var mm = BoolF64HashMap.init(std.testing.allocator);
    defer mm.deinit();
    _ = mm.put(true, 1.0);
    _ = mm.put(false, 2.0);
    var im = ImmutableBoolF64HashMap.fromMutable(std.testing.allocator, &mm);
    defer im.deinit();
    try std.testing.expectEqual(@as(?f64, 1.0), im.get(true));
    try std.testing.expectEqual(@as(usize, 2), im.len());
}

test "ImmutableBoolF64HashMap: toMutable independence" {
    var mm = BoolF64HashMap.init(std.testing.allocator);
    _ = mm.put(true, 1.0);
    var im = ImmutableBoolF64HashMap.fromMutable(std.testing.allocator, &mm);
    mm.deinit();
    defer im.deinit();
    var mm2 = im.toMutable();
    defer mm2.deinit();
    _ = mm2.put(false, 2.0);
    try std.testing.expectEqual(@as(usize, 1), im.len());
    try std.testing.expectEqual(@as(usize, 2), mm2.len());
}
