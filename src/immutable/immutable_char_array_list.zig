// Code generated by mapdb-codegen. DO NOT EDIT.

const std = @import("std");
const Allocator = std.mem.Allocator;
const CharArrayList = @import("../arraylist/char_array_list.zig").CharArrayList;

/// Immutable list of `u21` values, backed by an owned allocated slice.
///
/// All operations are read-only. To modify, call `toMutable()` to get
/// an independent mutable copy. The caller must call `deinit()` when done.
///
/// Go equivalent:   ImmutableCharArrayList
/// Rust equivalent:  ImmutableCharArrayList (Arc-backed)
pub const ImmutableCharArrayList = struct {
    items: []const u21,
    allocator: Allocator,

    /// Create from a slice by copying it. Caller retains ownership of the input.
    pub fn of(allocator: Allocator, values: []const u21) ImmutableCharArrayList {
        const owned = allocator.dupe(u21, values) catch @panic("out of memory");
        return .{ .items = owned, .allocator = allocator };
    }

    /// Create from a mutable list by taking a snapshot.
    pub fn fromMutable(allocator: Allocator, mutable: *const CharArrayList) ImmutableCharArrayList {
        return of(allocator, mutable.items.items);
    }

    /// Release the owned slice.
    pub fn deinit(self: *ImmutableCharArrayList) void {
        self.allocator.free(self.items);
    }

    pub fn get(self: *const ImmutableCharArrayList, index: usize) ?u21 {
        if (index >= self.items.len) return null;
        return self.items[index];
    }

    pub fn len(self: *const ImmutableCharArrayList) usize {
        return self.items.len;
    }

    pub fn isEmpty(self: *const ImmutableCharArrayList) bool {
        return self.items.len == 0;
    }

    pub fn contains(self: *const ImmutableCharArrayList, value: u21) bool {
        for (self.items) |item| {
            if (item == value) return true;
        }
        return false;
    }

    pub fn indexOf(self: *const ImmutableCharArrayList, value: u21) ?usize {
        for (self.items, 0..) |item, i| {
            if (item == value) return i;
        }
        return null;
    }

    pub fn toSlice(self: *const ImmutableCharArrayList) []const u21 {
        return self.items;
    }

    /// Returns the sum of all elements.
    pub fn sum(self: *const ImmutableCharArrayList) i64 {
        var total: i64 = 0;
        for (self.items) |item| {
            total += @as(i64, @intCast(item));
        }
        return total;
    }

    /// Returns the minimum element, or null if empty.
    pub fn min(self: *const ImmutableCharArrayList) ?u21 {
        if (self.items.len == 0) return null;
        var result = self.items[0];
        for (self.items[1..]) |item| {
            if (std.math.order(item, result) == .lt) result = item;
        }
        return result;
    }

    /// Returns the maximum element, or null if empty.
    pub fn max(self: *const ImmutableCharArrayList) ?u21 {
        if (self.items.len == 0) return null;
        var result = self.items[0];
        for (self.items[1..]) |item| {
            if (std.math.order(item, result) == .gt) result = item;
        }
        return result;
    }

    pub fn anySatisfy(self: *const ImmutableCharArrayList, predicate: *const fn (u21) bool) bool {
        for (self.items) |item| {
            if (predicate(item)) return true;
        }
        return false;
    }

    pub fn allSatisfy(self: *const ImmutableCharArrayList, predicate: *const fn (u21) bool) bool {
        for (self.items) |item| {
            if (!predicate(item)) return false;
        }
        return true;
    }

    pub fn noneSatisfy(self: *const ImmutableCharArrayList, predicate: *const fn (u21) bool) bool {
        for (self.items) |item| {
            if (predicate(item)) return false;
        }
        return true;
    }

    /// Create an independent mutable copy.
    pub fn toMutable(self: *const ImmutableCharArrayList) CharArrayList {
        return CharArrayList.of(self.allocator, self.items);
    }

    pub fn eql(self: *const ImmutableCharArrayList, other: *const ImmutableCharArrayList) bool {
        if (self.items.len != other.items.len) return false;
        for (self.items, other.items) |a, b| {
            if (!(a == b)) return false;
        }
        return true;
    }
};

test "ImmutableCharArrayList: of and get" {
    var il = ImmutableCharArrayList.of(std.testing.allocator, &[_]u21{ 'a', 'b', 'c' });
    defer il.deinit();
    try std.testing.expectEqual(@as(usize, 3), il.len());
    try std.testing.expectEqual(@as(?u21, 'a'), il.get(0));
    try std.testing.expectEqual(@as(?u21, null), il.get(99));
}

test "ImmutableCharArrayList: contains" {
    var il = ImmutableCharArrayList.of(std.testing.allocator, &[_]u21{ 'a', 'b' });
    defer il.deinit();
    try std.testing.expect(il.contains('a'));
    try std.testing.expect(!il.contains('z'));
}

test "ImmutableCharArrayList: fromMutable" {
    var ml = CharArrayList.of(std.testing.allocator, &[_]u21{ 'a', 'b' });
    defer ml.deinit();
    var il = ImmutableCharArrayList.fromMutable(std.testing.allocator, &ml);
    defer il.deinit();
    try std.testing.expectEqual(@as(usize, 2), il.len());
}

test "ImmutableCharArrayList: toMutable independence" {
    var il = ImmutableCharArrayList.of(std.testing.allocator, &[_]u21{ 'a', 'b' });
    defer il.deinit();
    var ml = il.toMutable();
    defer ml.deinit();
    ml.push('c');
    // Immutable list unchanged
    try std.testing.expectEqual(@as(usize, 2), il.len());
    try std.testing.expectEqual(@as(usize, 3), ml.len());
}

test "ImmutableCharArrayList: eql" {
    var a = ImmutableCharArrayList.of(std.testing.allocator, &[_]u21{ 'a', 'b' });
    defer a.deinit();
    var b = ImmutableCharArrayList.of(std.testing.allocator, &[_]u21{ 'a', 'b' });
    defer b.deinit();
    try std.testing.expect(a.eql(&b));
}
