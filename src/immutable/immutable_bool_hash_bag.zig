// Code generated by mapdb-codegen. DO NOT EDIT.

const std = @import("std");
const Allocator = std.mem.Allocator;
const OpenHashMap = @import("../hash_table.zig").OpenHashMap;
const BoolHashBag = @import("../bag/bool_hash_bag.zig").BoolHashBag;

/// Immutable bag (multiset) of `bool` values with occurrence counting.
///
/// Backed by a snapshot of the counts map. All operations are read-only.
pub const ImmutableBoolHashBag = struct {
    counts: OpenHashMap(bool, usize),
    size: usize,
    allocator: Allocator,

    pub fn of(allocator: Allocator, values: []const bool) ImmutableBoolHashBag {
        var mutable = BoolHashBag.init(allocator);
        for (values) |val| mutable.add(val);
        const result = fromMutable(allocator, &mutable);
        mutable.deinit();
        return result;
    }

    pub fn fromMutable(allocator: Allocator, mutable: *const BoolHashBag) ImmutableBoolHashBag {
        var counts = OpenHashMap(bool, usize).init(allocator, allocator, allocator) catch @panic("out of memory");
        for (0..mutable.counts.capacity) |i| {
            if (mutable.counts.entries[i].occupied) {
                _ = counts.put(mutable.counts.entries[i].key, mutable.counts.entries[i].value) catch @panic("out of memory");
            }
        }
        return .{
            .counts = counts,
            .size = mutable.size,
            .allocator = allocator,
        };
    }

    pub fn deinit(self: *ImmutableBoolHashBag) void {
        self.counts.deinit();
    }

    pub fn occurrencesOf(self: *const ImmutableBoolHashBag, value: bool) usize {
        return self.counts.get(value) orelse 0;
    }

    pub fn contains(self: *const ImmutableBoolHashBag, value: bool) bool {
        return self.occurrencesOf(value) > 0;
    }

    pub fn totalSize(self: *const ImmutableBoolHashBag) usize {
        return self.size;
    }

    pub fn sizeDistinct(self: *const ImmutableBoolHashBag) usize {
        return self.counts.len();
    }

    pub fn isEmpty(self: *const ImmutableBoolHashBag) bool {
        return self.size == 0;
    }

    pub fn len(self: *const ImmutableBoolHashBag) usize {
        return self.size;
    }

    pub fn toSlice(self: *const ImmutableBoolHashBag) []const bool {
        _ = self;
        // Not directly applicable for bags; use toMutable().
        return &[_]bool{};
    }

    pub fn toMutable(self: *const ImmutableBoolHashBag) BoolHashBag {
        var mutable = BoolHashBag.init(self.allocator);
        for (0..self.counts.capacity) |i| {
            if (self.counts.entries[i].occupied) {
                var j: usize = 0;
                while (j < self.counts.entries[i].value) : (j += 1) {
                    mutable.add(self.counts.entries[i].key);
                }
            }
        }
        return mutable;
    }
};

test "ImmutableBoolHashBag: of and occurrences" {
    var ib = ImmutableBoolHashBag.of(std.testing.allocator, &[_]bool{ true, true, false });
    defer ib.deinit();
    try std.testing.expectEqual(@as(usize, 2), ib.occurrencesOf(true));
    try std.testing.expectEqual(@as(usize, 1), ib.occurrencesOf(false));
    try std.testing.expectEqual(@as(usize, 3), ib.totalSize());
    try std.testing.expectEqual(@as(usize, 2), ib.sizeDistinct());
}

test "ImmutableBoolHashBag: toMutable independence" {
    var ib = ImmutableBoolHashBag.of(std.testing.allocator, &[_]bool{ true, true });
    defer ib.deinit();
    var mb = ib.toMutable();
    defer mb.deinit();
    mb.add(false);
    try std.testing.expectEqual(@as(usize, 2), ib.totalSize());
    try std.testing.expectEqual(@as(usize, 3), mb.totalSize());
}
