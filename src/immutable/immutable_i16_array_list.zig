// Code generated by mapdb-codegen. DO NOT EDIT.

const std = @import("std");
const Allocator = std.mem.Allocator;
const I16ArrayList = @import("../arraylist/i16_array_list.zig").I16ArrayList;

/// Immutable list of `i16` values, backed by an owned allocated slice.
///
/// All operations are read-only. To modify, call `toMutable()` to get
/// an independent mutable copy. The caller must call `deinit()` when done.
///
/// Go equivalent:   ImmutableI16ArrayList
/// Rust equivalent:  ImmutableI16ArrayList (Arc-backed)
pub const ImmutableI16ArrayList = struct {
    items: []const i16,
    allocator: Allocator,

    /// Create from a slice by copying it. Caller retains ownership of the input.
    pub fn of(allocator: Allocator, values: []const i16) ImmutableI16ArrayList {
        const owned = allocator.dupe(i16, values) catch @panic("out of memory");
        return .{ .items = owned, .allocator = allocator };
    }

    /// Create from a mutable list by taking a snapshot.
    pub fn fromMutable(allocator: Allocator, mutable: *const I16ArrayList) ImmutableI16ArrayList {
        return of(allocator, mutable.items.items);
    }

    /// Release the owned slice.
    pub fn deinit(self: *ImmutableI16ArrayList) void {
        self.allocator.free(self.items);
    }

    pub fn get(self: *const ImmutableI16ArrayList, index: usize) ?i16 {
        if (index >= self.items.len) return null;
        return self.items[index];
    }

    pub fn len(self: *const ImmutableI16ArrayList) usize {
        return self.items.len;
    }

    pub fn isEmpty(self: *const ImmutableI16ArrayList) bool {
        return self.items.len == 0;
    }

    pub fn contains(self: *const ImmutableI16ArrayList, value: i16) bool {
        for (self.items) |item| {
            if (item == value) return true;
        }
        return false;
    }

    pub fn indexOf(self: *const ImmutableI16ArrayList, value: i16) ?usize {
        for (self.items, 0..) |item, i| {
            if (item == value) return i;
        }
        return null;
    }

    pub fn toSlice(self: *const ImmutableI16ArrayList) []const i16 {
        return self.items;
    }

    /// Returns the sum of all elements.
    pub fn sum(self: *const ImmutableI16ArrayList) i64 {
        var total: i64 = 0;
        for (self.items) |item| {
            total += @as(i64, @intCast(item));
        }
        return total;
    }

    /// Returns the minimum element, or null if empty.
    pub fn min(self: *const ImmutableI16ArrayList) ?i16 {
        if (self.items.len == 0) return null;
        var result = self.items[0];
        for (self.items[1..]) |item| {
            if (std.math.order(item, result) == .lt) result = item;
        }
        return result;
    }

    /// Returns the maximum element, or null if empty.
    pub fn max(self: *const ImmutableI16ArrayList) ?i16 {
        if (self.items.len == 0) return null;
        var result = self.items[0];
        for (self.items[1..]) |item| {
            if (std.math.order(item, result) == .gt) result = item;
        }
        return result;
    }

    pub fn anySatisfy(self: *const ImmutableI16ArrayList, predicate: *const fn (i16) bool) bool {
        for (self.items) |item| {
            if (predicate(item)) return true;
        }
        return false;
    }

    pub fn allSatisfy(self: *const ImmutableI16ArrayList, predicate: *const fn (i16) bool) bool {
        for (self.items) |item| {
            if (!predicate(item)) return false;
        }
        return true;
    }

    pub fn noneSatisfy(self: *const ImmutableI16ArrayList, predicate: *const fn (i16) bool) bool {
        for (self.items) |item| {
            if (predicate(item)) return false;
        }
        return true;
    }

    /// Create an independent mutable copy.
    pub fn toMutable(self: *const ImmutableI16ArrayList) I16ArrayList {
        return I16ArrayList.of(self.allocator, self.items);
    }

    pub fn eql(self: *const ImmutableI16ArrayList, other: *const ImmutableI16ArrayList) bool {
        if (self.items.len != other.items.len) return false;
        for (self.items, other.items) |a, b| {
            if (!(a == b)) return false;
        }
        return true;
    }
};

test "ImmutableI16ArrayList: of and get" {
    var il = ImmutableI16ArrayList.of(std.testing.allocator, &[_]i16{ 1, 2, 3 });
    defer il.deinit();
    try std.testing.expectEqual(@as(usize, 3), il.len());
    try std.testing.expectEqual(@as(?i16, 1), il.get(0));
    try std.testing.expectEqual(@as(?i16, null), il.get(99));
}

test "ImmutableI16ArrayList: contains" {
    var il = ImmutableI16ArrayList.of(std.testing.allocator, &[_]i16{ 1, 2 });
    defer il.deinit();
    try std.testing.expect(il.contains(1));
    try std.testing.expect(!il.contains(99));
}

test "ImmutableI16ArrayList: fromMutable" {
    var ml = I16ArrayList.of(std.testing.allocator, &[_]i16{ 1, 2 });
    defer ml.deinit();
    var il = ImmutableI16ArrayList.fromMutable(std.testing.allocator, &ml);
    defer il.deinit();
    try std.testing.expectEqual(@as(usize, 2), il.len());
}

test "ImmutableI16ArrayList: toMutable independence" {
    var il = ImmutableI16ArrayList.of(std.testing.allocator, &[_]i16{ 1, 2 });
    defer il.deinit();
    var ml = il.toMutable();
    defer ml.deinit();
    ml.push(3);
    // Immutable list unchanged
    try std.testing.expectEqual(@as(usize, 2), il.len());
    try std.testing.expectEqual(@as(usize, 3), ml.len());
}

test "ImmutableI16ArrayList: eql" {
    var a = ImmutableI16ArrayList.of(std.testing.allocator, &[_]i16{ 1, 2 });
    defer a.deinit();
    var b = ImmutableI16ArrayList.of(std.testing.allocator, &[_]i16{ 1, 2 });
    defer b.deinit();
    try std.testing.expect(a.eql(&b));
}
